[pytest]
pythonpath = .
markers =
    unit: fast pure logic coverage
    api: request or response level API coverage
    integration: multi-component runtime coverage
    slow: intentionally slower coverage
